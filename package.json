{
  "name": "mern-task-manager",
  "version": "1.0.0",
  "description": "A MERN application  tasks management",
  "main": "index.js",
  "scripts": {
    "server": "npm start --prefix backend",
    "client": "npm start --prefix frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "build": "npm install --prefix frontend && npm install --prefix backend && npm run build --prefix frontend",
    "start": "npm run build && npm run start --prefix backend"
  },
  "keywords": ["MERN", "Tasks","Task manager", "Full stack application"],
  "author": "Santhosh",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.1.0"
  }
}